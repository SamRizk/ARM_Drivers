
#include "../include/MCLA/RCC/RCC.h"

int
main(void)
{

	 u32 S = BIT_BAND(0x40023800,1);
	// At this stage the system clock should have already been configured
  // at high speed.

  // Infinite loop
  while (1)
    {
       // Add your code here.
    }
}
